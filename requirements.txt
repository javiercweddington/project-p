requests>=2.31.0
numpy>=1.24.0
pandas>=2.0.0

# PDF: PyMuPDF drives both the raster and redact strategies; pypdf is the
# fallback reader (code imports `pypdf`, NOT the deprecated PyPDF2).
PyMuPDF>=1.24.0
pypdf>=4.0.0

# Office formats
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
olefile>=0.46

# Images + OCR: RapidOCR is the primary backend (GPU-capable via
# onnxruntime-gpu; set PROJECT_P_OCR_DEVICE=cuda). pytesseract is the
# fallback and needs the tesseract binary + chi_sim traineddata.
pillow>=10.0.0
rapidocr-onnxruntime>=1.3.0
pytesseract>=0.3.10

# NER
gliner>=0.2.0
